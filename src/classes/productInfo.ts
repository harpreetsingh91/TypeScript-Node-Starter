export class ProductInfo {
    productName: string;
    productInfo: string;

    constructor(productName: string, productInfo: string) {
        this.productName = productName;
        this.productInfo = productInfo;
    }
}
